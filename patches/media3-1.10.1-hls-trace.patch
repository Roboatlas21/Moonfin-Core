diff --git a/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsChunkSource.java b/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsChunkSource.java
index d527f52..602bcf3 100644
--- a/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsChunkSource.java
+++ b/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsChunkSource.java
@@ -31,6 +31,7 @@ import androidx.annotation.VisibleForTesting;
 import androidx.media3.common.C;
 import androidx.media3.common.Format;
 import androidx.media3.common.TrackGroup;
+import androidx.media3.common.util.Log;
 import androidx.media3.common.util.TimestampAdjuster;
 import androidx.media3.common.util.UriUtil;
 import androidx.media3.common.util.Util;
@@ -442,6 +443,38 @@ import org.checkerframework.checker.nullness.qual.MonotonicNonNull;
       boolean allowEndOfStream,
       HlsChunkHolder out) {
     @Nullable HlsMediaChunk previous = queue.isEmpty() ? null : Iterables.getLast(queue);
+
+    boolean traceSchedule =
+        previous == null || previous.chunkIndex <= 1;
+
+    if (traceSchedule) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "CHUNK_SOURCE_ENTER"
+              + " previousSeq="
+              + (previous == null
+                  ? "none"
+                  : previous.chunkIndex)
+              + " previousCompleted="
+              + (previous == null
+                  ? "n/a"
+                  : previous.isLoadCompleted())
+              + " previousPublished="
+              + (previous == null
+                  ? "n/a"
+                  : previous.isPublished())
+              + " playbackUs="
+              + loadingInfo.playbackPositionUs
+              + " loadPositionUs="
+              + loadPositionUs
+              + " largestReadUs="
+              + largestReadPositionUs
+              + " queueSize="
+              + queue.size()
+              + " allowEndOfStream="
+              + allowEndOfStream);
+    }
+
     int oldTrackIndex = previous == null ? C.INDEX_UNSET : trackGroup.indexOf(previous.trackFormat);
     long playbackPositionUs = loadingInfo.playbackPositionUs;
     long bufferedDurationUs = loadPositionUs - playbackPositionUs;
@@ -473,6 +506,15 @@ import org.checkerframework.checker.nullness.qual.MonotonicNonNull;
     boolean switchingTrack = oldTrackIndex != selectedTrackIndex;
     Uri selectedPlaylistUrl = redundantGroups[selectedTrackIndex].getCurrentPlaylistUrl();
     if (!playlistTracker.isSnapshotValid(selectedPlaylistUrl)) {
+      if (traceSchedule) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "CHUNK_SOURCE_WAIT"
+                + " reason=invalidPlaylistSnapshot"
+                + " selectedTrack="
+                + selectedTrackIndex);
+      }
+
       out.playlistUrl = selectedPlaylistUrl;
       nextChunkStuckOnPlaylistUrl = selectedPlaylistUrl;
       // Retry when playlist is refreshed.
@@ -524,6 +566,36 @@ import org.checkerframework.checker.nullness.qual.MonotonicNonNull;
       partIndex = nextMediaSequenceAndPartIndexWithoutAdapting.second;
     }
 
+    if (traceSchedule || chunkMediaSequence <= 2) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "CHUNK_SOURCE_SELECTED"
+              + " previousSeq="
+              + (previous == null
+                  ? "none"
+                  : previous.chunkIndex)
+              + " nextSeq="
+              + chunkMediaSequence
+              + " partIndex="
+              + partIndex
+              + " playlistMediaSequence="
+              + playlist.mediaSequence
+              + " playlistSegments="
+              + playlist.segments.size()
+              + " hasEndTag="
+              + playlist.hasEndTag
+              + " independentSegments="
+              + playlist.hasIndependentSegments
+              + " oldTrack="
+              + oldTrackIndex
+              + " selectedTrack="
+              + selectedTrackIndex
+              + " switchingTrack="
+              + switchingTrack
+              + " forcedKeep="
+              + shouldForceKeepCurrentTrackSelection);
+    }
+
     // If the selected track index changes from another one, we should deactivate the old
     // playlist for playback.
     if (selectedTrackIndex != oldTrackIndex && oldTrackIndex != C.INDEX_UNSET) {
@@ -532,6 +604,17 @@ import org.checkerframework.checker.nullness.qual.MonotonicNonNull;
     }
 
     if (chunkMediaSequence < playlist.mediaSequence) {
+      if (traceSchedule) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "CHUNK_SOURCE_STOP"
+                + " reason=behindLiveWindow"
+                + " nextSeq="
+                + chunkMediaSequence
+                + " playlistMediaSequence="
+                + playlist.mediaSequence);
+      }
+
       fatalError = new BehindLiveWindowException();
       return;
     }
@@ -541,11 +624,33 @@ import org.checkerframework.checker.nullness.qual.MonotonicNonNull;
         getNextSegmentHolder(playlist, chunkMediaSequence, partIndex);
     if (segmentBaseHolder == null) {
       if (!playlist.hasEndTag) {
+        if (traceSchedule) {
+          Log.d(
+              "MF_HLS_SCHED",
+              "CHUNK_SOURCE_WAIT"
+                  + " reason=segmentNotAvailableRefreshPlaylist"
+                  + " nextSeq="
+                  + chunkMediaSequence);
+        }
+
         // Reload the playlist in case of a live stream.
         out.playlistUrl = selectedPlaylistUrl;
         nextChunkStuckOnPlaylistUrl = selectedPlaylistUrl;
         return;
       } else if (allowEndOfStream || playlist.segments.isEmpty()) {
+        if (traceSchedule) {
+          Log.d(
+              "MF_HLS_SCHED",
+              "CHUNK_SOURCE_STOP"
+                  + " reason=endOfStream"
+                  + " nextSeq="
+                  + chunkMediaSequence
+                  + " allowEndOfStream="
+                  + allowEndOfStream
+                  + " playlistSegments="
+                  + playlist.segments.size());
+        }
+
         out.endOfStream = true;
         return;
       }
@@ -626,6 +731,17 @@ import org.checkerframework.checker.nullness.qual.MonotonicNonNull;
             segmentBaseHolder,
             startOfPlaylistInPeriodUs);
     if (shouldSpliceIn && segmentBaseHolder.isPreload) {
+      if (traceSchedule) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "CHUNK_SOURCE_WAIT"
+                + " reason=splicePreload"
+                + " nextSeq="
+                + chunkMediaSequence
+                + " partIndex="
+                + partIndex);
+      }
+
       // We don't support discarding spliced-in segments [internal: b/159904763], but preload
       // parts may need to be discarded if they are removed before becoming permanently published.
       // Hence, don't allow this combination and instead wait with loading the next part until it
@@ -655,6 +771,21 @@ import org.checkerframework.checker.nullness.qual.MonotonicNonNull;
             isIndependent,
             playerId,
             cmcdDataFactory);
+
+    if (traceSchedule || chunkMediaSequence <= 2) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "CHUNK_SOURCE_RESULT"
+              + " result=mediaChunk"
+              + " seq="
+              + chunkMediaSequence
+              + " partIndex="
+              + partIndex
+              + " splice="
+              + shouldSpliceIn
+              + " independent="
+              + isIndependent);
+    }
   }
 
   private static boolean isIndependent(
diff --git a/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsMediaChunk.java b/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsMediaChunk.java
index 188b9de..af1781d 100644
--- a/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsMediaChunk.java
+++ b/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsMediaChunk.java
@@ -25,6 +25,7 @@ import androidx.media3.common.C;
 import androidx.media3.common.DrmInitData;
 import androidx.media3.common.Format;
 import androidx.media3.common.Metadata;
+import androidx.media3.common.util.Log;
 import androidx.media3.common.util.ParsableByteArray;
 import androidx.media3.common.util.TimestampAdjuster;
 import androidx.media3.common.util.UriUtil;
@@ -39,6 +40,7 @@ import androidx.media3.extractor.DefaultExtractorInput;
 import androidx.media3.extractor.ExtractorInput;
 import androidx.media3.extractor.metadata.id3.Id3Decoder;
 import androidx.media3.extractor.metadata.id3.PrivFrame;
+import androidx.media3.extractor.ts.TsExtractor;
 import com.google.common.base.Ascii;
 import com.google.common.collect.ImmutableList;
 import java.io.EOFException;
@@ -434,16 +436,55 @@ import org.checkerframework.checker.nullness.qual.RequiresNonNull;
   public void load() throws IOException {
     // output == null means init() hasn't been called.
     checkNotNull(output);
+    long traceStartMs = System.currentTimeMillis();
+    boolean extractorReused = false;
     if (extractor == null && previousExtractor != null && previousExtractor.isReusable()) {
       extractor = previousExtractor;
       initDataLoadRequired = false;
+      extractorReused = true;
     }
-    maybeLoadInitData();
-    if (!loadCanceled) {
-      if (!hasGapTag) {
-        loadMedia();
+    Log.d(
+        "MF_HLS_TRACE",
+        "LOAD START"
+            + " seq=" + chunkIndex
+            + " uri=" + dataSpec.uri
+            + " startUs=" + startTimeUs
+            + " endUs=" + endTimeUs
+            + " previousExtractor=" + (previousExtractor != null)
+            + " extractorReused=" + extractorReused
+            + " extractorBefore="
+            + (extractor != null ? extractor.getClass().getSimpleName() : "null"));
+    try {
+      maybeLoadInitData();
+      if (!loadCanceled) {
+        if (!hasGapTag) {
+          loadMedia();
+        }
+        loadCompleted = !loadCanceled;
       }
-      loadCompleted = !loadCanceled;
+    } catch (Exception e) {
+      Log.e(
+          "MF_HLS_TRACE",
+          "LOAD ERROR"
+              + " seq=" + chunkIndex
+              + " uri=" + dataSpec.uri
+              + " exception=" + e.getClass().getSimpleName()
+              + " message=" + e.getMessage(),
+          e);
+      throw e;
+    } finally {
+      Log.d(
+          "MF_HLS_TRACE",
+          "LOAD END"
+              + " seq=" + chunkIndex
+              + " uri=" + dataSpec.uri
+              + " completed=" + loadCompleted
+              + " canceled=" + loadCanceled
+              + " nextLoadPosition=" + nextLoadPosition
+              + " extractor="
+              + (extractor != null ? extractor.getClass().getSimpleName() : "null")
+              + " extractorReused=" + extractorReused
+              + " elapsedMs=" + (System.currentTimeMillis() - traceStartMs));
     }
   }
 
@@ -497,8 +538,13 @@ import org.checkerframework.checker.nullness.qual.RequiresNonNull;
 
   @RequiresNonNull("output")
   private void loadMedia() throws IOException {
-    feedDataToExtractor(
-        dataSource, dataSpec, mediaSegmentEncrypted, /* initializeTimestampAdjuster= */ true);
+    TsExtractor.debugBeginHlsSegment(chunkIndex, dataSpec.uri.toString());
+    try {
+      feedDataToExtractor(
+          dataSource, dataSpec, mediaSegmentEncrypted, /* initializeTimestampAdjuster= */ true);
+    } finally {
+      TsExtractor.debugEndHlsSegment();
+    }
   }
 
   /**
diff --git a/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsMediaPeriod.java b/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsMediaPeriod.java
index 43f0fa3..e5105d2 100644
--- a/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsMediaPeriod.java
+++ b/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsMediaPeriod.java
@@ -29,6 +29,7 @@ import androidx.media3.common.Metadata;
 import androidx.media3.common.MimeTypes;
 import androidx.media3.common.StreamKey;
 import androidx.media3.common.TrackGroup;
+import androidx.media3.common.util.Log;
 import androidx.media3.common.util.NullableType;
 import androidx.media3.common.util.Util;
 import androidx.media3.datasource.DataSource;
@@ -441,15 +442,49 @@ import org.checkerframework.checker.nullness.qual.MonotonicNonNull;
 
   @Override
   public boolean continueLoading(LoadingInfo loadingInfo) {
+    boolean traceSchedule =
+        loadingInfo.playbackPositionUs <= 10_000_000L;
+
+    if (traceSchedule) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "PERIOD_CONTINUE_ENTER"
+              + " playbackUs="
+              + loadingInfo.playbackPositionUs
+              + " prepared="
+              + (trackGroups != null)
+              + " enabledWrappers="
+              + enabledSampleStreamWrappers.length
+              + " nextLoadUs="
+              + compositeSequenceableLoader.getNextLoadPositionUs());
+    }
+
+    boolean result;
+
     if (trackGroups == null) {
       // Preparation is still going on.
       for (HlsSampleStreamWrapper wrapper : sampleStreamWrappers) {
         wrapper.continuePreparing();
       }
-      return false;
+      result = false;
     } else {
-      return compositeSequenceableLoader.continueLoading(loadingInfo);
+      result =
+          compositeSequenceableLoader.continueLoading(loadingInfo);
     }
+
+    if (traceSchedule) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "PERIOD_CONTINUE_RETURN"
+              + " playbackUs="
+              + loadingInfo.playbackPositionUs
+              + " result="
+              + result
+              + " nextLoadUs="
+              + compositeSequenceableLoader.getNextLoadPositionUs());
+    }
+
+    return result;
   }
 
   @Override
@@ -1085,7 +1120,19 @@ import org.checkerframework.checker.nullness.qual.MonotonicNonNull;
 
     @Override
     public void onContinueLoadingRequested(HlsSampleStreamWrapper sampleStreamWrapper) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "PERIOD_CALLBACK_RECEIVED"
+              + " wrapperLoading="
+              + sampleStreamWrapper.isLoading()
+              + " nextLoadUs="
+              + sampleStreamWrapper.getNextLoadPositionUs());
+
       mediaPeriodCallback.onContinueLoadingRequested(HlsMediaPeriod.this);
+
+      Log.d(
+          "MF_HLS_SCHED",
+          "PERIOD_CALLBACK_FORWARD");
     }
   }
 }
diff --git a/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsSampleStreamWrapper.java b/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsSampleStreamWrapper.java
index 40ce0bf..1edf36a 100644
--- a/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsSampleStreamWrapper.java
+++ b/libraries/exoplayer_hls/src/main/java/androidx/media3/exoplayer/hls/HlsSampleStreamWrapper.java
@@ -793,7 +793,69 @@ import org.checkerframework.checker.nullness.qual.RequiresNonNull;
 
   @Override
   public boolean continueLoading(LoadingInfo loadingInfo) {
-    if (loadingFinished || loader.isLoading() || loader.hasFatalError()) {
+    @Nullable HlsMediaChunk traceLastChunk =
+        mediaChunks.isEmpty() ? null : getLastMediaChunk();
+
+    boolean traceSchedule =
+        traceLastChunk == null || traceLastChunk.chunkIndex <= 1;
+
+    boolean traceLoaderLoading = loader.isLoading();
+    boolean traceFatalError = loader.hasFatalError();
+
+    if (traceSchedule) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "CONTINUE_ENTER"
+              + " uid="
+              + uid
+              + " playbackUs="
+              + loadingInfo.playbackPositionUs
+              + " prepared="
+              + prepared
+              + " loadingFinished="
+              + loadingFinished
+              + " loaderLoading="
+              + traceLoaderLoading
+              + " fatalError="
+              + traceFatalError
+              + " pendingReset="
+              + isPendingReset()
+              + " enabledTrackGroups="
+              + enabledTrackGroupCount
+              + " queueSize="
+              + mediaChunks.size()
+              + " lastSeq="
+              + (traceLastChunk == null
+                  ? "none"
+                  : traceLastChunk.chunkIndex)
+              + " lastCompleted="
+              + (traceLastChunk == null
+                  ? "n/a"
+                  : traceLastChunk.isLoadCompleted())
+              + " lastPublished="
+              + (traceLastChunk == null
+                  ? "n/a"
+                  : traceLastChunk.isPublished()));
+    }
+
+    if (loadingFinished || traceLoaderLoading || traceFatalError) {
+      if (traceSchedule) {
+        String reason =
+            loadingFinished
+                ? "loadingFinished"
+                : traceLoaderLoading
+                    ? "loaderAlreadyLoading"
+                    : "fatalError";
+
+        Log.d(
+            "MF_HLS_SCHED",
+            "CONTINUE_SKIP"
+                + " uid="
+                + uid
+                + " reason="
+                + reason);
+      }
+
       return false;
     }
 
@@ -826,7 +888,26 @@ import org.checkerframework.checker.nullness.qual.RequiresNonNull;
         }
       }
     }
+    if (traceSchedule) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "GET_NEXT_CALL"
+              + " uid="
+              + uid
+              + " playbackUs="
+              + loadingInfo.playbackPositionUs
+              + " loadPositionUs="
+              + loadPositionUs
+              + " largestReadUs="
+              + largestReadPositionUs
+              + " queueSize="
+              + chunkQueue.size()
+              + " allowEndOfStream="
+              + (prepared || !chunkQueue.isEmpty()));
+    }
+
     nextChunkHolder.clear();
+
     chunkSource.getNextChunk(
         loadingInfo,
         loadPositionUs,
@@ -838,26 +919,138 @@ import org.checkerframework.checker.nullness.qual.RequiresNonNull;
     @Nullable Chunk loadable = nextChunkHolder.chunk;
     @Nullable Uri playlistUrlToLoad = nextChunkHolder.playlistUrl;
 
+    long traceResultSeq =
+        loadable instanceof HlsMediaChunk
+            ? ((HlsMediaChunk) loadable).chunkIndex
+            : C.INDEX_UNSET;
+
+    boolean traceResult =
+        traceSchedule
+            || (traceResultSeq != C.INDEX_UNSET
+                && traceResultSeq <= 2);
+
+    if (traceResult) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "GET_NEXT_RESULT"
+              + " uid="
+              + uid
+              + " endOfStream="
+              + endOfStream
+              + " loadable="
+              + (loadable == null
+                  ? "null"
+                  : loadable.getClass().getSimpleName())
+              + " seq="
+              + (traceResultSeq == C.INDEX_UNSET
+                  ? "n/a"
+                  : traceResultSeq)
+              + " playlistRefresh="
+              + (playlistUrlToLoad != null));
+    }
+
     if (endOfStream) {
+      if (traceResult) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "CONTINUE_RESULT"
+                + " uid="
+                + uid
+                + " result=endOfStream");
+      }
+
       pendingResetPositionUs = C.TIME_UNSET;
       loadingFinished = true;
       return true;
     }
 
     if (loadable == null) {
+      if (traceResult) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "CONTINUE_RESULT"
+                + " uid="
+                + uid
+                + " result=noChunk"
+                + " playlistRefresh="
+                + (playlistUrlToLoad != null));
+      }
+
       if (playlistUrlToLoad != null) {
         callback.onPlaylistRefreshRequired(playlistUrlToLoad);
       }
+
       return false;
     }
 
     if (isMediaChunk(loadable)) {
+      if (traceResult) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "MEDIA_CHUNK_INIT_CALL"
+                + " uid="
+                + uid
+                + " seq="
+                + traceResultSeq);
+      }
+
       maybeDiscardUpstreamForNewMediaChunk((HlsMediaChunk) loadable);
       initMediaChunkLoad((HlsMediaChunk) loadable);
+
+      if (traceResult) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "MEDIA_CHUNK_INIT_RETURN"
+                + " uid="
+                + uid
+                + " seq="
+                + traceResultSeq
+                + " queueSize="
+                + mediaChunks.size());
+      }
     }
+
     loadingChunk = loadable;
-    loader.startLoading(
-        loadable, this, loadErrorHandlingPolicy.getMinimumLoadableRetryCount(loadable.type));
+    int minimumRetryCount =
+        loadErrorHandlingPolicy.getMinimumLoadableRetryCount(loadable.type);
+
+    if (traceResult) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "LOADER_START_CALL"
+              + " uid="
+              + uid
+              + " loadable="
+              + loadable.getClass().getSimpleName()
+              + " seq="
+              + (traceResultSeq == C.INDEX_UNSET
+                  ? "n/a"
+                  : traceResultSeq)
+              + " loaderLoadingBefore="
+              + loader.isLoading()
+              + " retryCount="
+              + minimumRetryCount);
+    }
+
+    long loaderStartRealtimeMs =
+        loader.startLoading(loadable, this, minimumRetryCount);
+
+    if (traceResult) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "LOADER_START_RETURN"
+              + " uid="
+              + uid
+              + " seq="
+              + (traceResultSeq == C.INDEX_UNSET
+                  ? "n/a"
+                  : traceResultSeq)
+              + " loaderLoadingAfter="
+              + loader.isLoading()
+              + " startRealtimeMs="
+              + loaderStartRealtimeMs);
+    }
+
     return true;
   }
 
@@ -933,6 +1126,30 @@ import org.checkerframework.checker.nullness.qual.RequiresNonNull;
   @Override
   public void onLoadStarted(
       Chunk loadable, long elapsedRealtimeMs, long loadDurationMs, int retryCount) {
+
+    long startedSeq =
+        loadable instanceof HlsMediaChunk
+            ? ((HlsMediaChunk) loadable).chunkIndex
+            : C.INDEX_UNSET;
+
+    if (startedSeq == C.INDEX_UNSET || startedSeq <= 1) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "LOAD_CALLBACK_STARTED"
+              + " uid="
+              + uid
+              + " loadable="
+              + loadable.getClass().getSimpleName()
+              + " seq="
+              + (startedSeq == C.INDEX_UNSET
+                  ? "n/a"
+                  : startedSeq)
+              + " retryCount="
+              + retryCount
+              + " durationMs="
+              + loadDurationMs);
+    }
+
     LoadEventInfo loadEventInfo =
         retryCount == 0
             ? new LoadEventInfo(loadable.loadTaskId, loadable.dataSpec, elapsedRealtimeMs)
@@ -960,6 +1177,43 @@ import org.checkerframework.checker.nullness.qual.RequiresNonNull;
   public void onLoadCompleted(Chunk loadable, long elapsedRealtimeMs, long loadDurationMs) {
     loadingChunk = null;
     chunkSource.onChunkLoadCompleted(loadable);
+
+    long completedSeq =
+        loadable instanceof HlsMediaChunk
+            ? ((HlsMediaChunk) loadable).chunkIndex
+            : C.INDEX_UNSET;
+
+    boolean traceCompleted =
+        completedSeq == C.INDEX_UNSET || completedSeq <= 1;
+
+    if (traceCompleted) {
+      Log.d(
+          "MF_HLS_SCHED",
+          "LOAD_CALLBACK_COMPLETED"
+              + " uid="
+              + uid
+              + " loadable="
+              + loadable.getClass().getSimpleName()
+              + " seq="
+              + (completedSeq == C.INDEX_UNSET
+                  ? "n/a"
+                  : completedSeq)
+              + " prepared="
+              + prepared
+              + " loaderLoading="
+              + loader.isLoading()
+              + " loadingFinished="
+              + loadingFinished
+              + " pendingReset="
+              + isPendingReset()
+              + " enabledTrackGroups="
+              + enabledTrackGroupCount
+              + " queueSize="
+              + mediaChunks.size()
+              + " durationMs="
+              + loadDurationMs);
+    }
+
     LoadEventInfo loadEventInfo =
         new LoadEventInfo(
             loadable.loadTaskId,
@@ -980,9 +1234,67 @@ import org.checkerframework.checker.nullness.qual.RequiresNonNull;
         loadable.startTimeUs,
         loadable.endTimeUs);
     if (!prepared) {
-      continueLoading(new LoadingInfo.Builder().setPlaybackPositionUs(lastSeekPositionUs).build());
+      if (traceCompleted) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "NEXT_TRIGGER"
+                + " uid="
+                + uid
+                + " seq="
+                + (completedSeq == C.INDEX_UNSET
+                    ? "n/a"
+                    : completedSeq)
+                + " mode=directContinueLoading");
+      }
+
+      boolean continueResult =
+          continueLoading(
+              new LoadingInfo.Builder()
+                  .setPlaybackPositionUs(lastSeekPositionUs)
+                  .build());
+
+      if (traceCompleted) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "NEXT_TRIGGER_RETURN"
+                + " uid="
+                + uid
+                + " seq="
+                + (completedSeq == C.INDEX_UNSET
+                    ? "n/a"
+                    : completedSeq)
+                + " mode=directContinueLoading"
+                + " result="
+                + continueResult);
+      }
     } else {
+      if (traceCompleted) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "NEXT_TRIGGER"
+                + " uid="
+                + uid
+                + " seq="
+                + (completedSeq == C.INDEX_UNSET
+                    ? "n/a"
+                    : completedSeq)
+                + " mode=callback");
+      }
+
       callback.onContinueLoadingRequested(this);
+
+      if (traceCompleted) {
+        Log.d(
+            "MF_HLS_SCHED",
+            "NEXT_TRIGGER_RETURN"
+                + " uid="
+                + uid
+                + " seq="
+                + (completedSeq == C.INDEX_UNSET
+                    ? "n/a"
+                    : completedSeq)
+                + " mode=callback");
+      }
     }
   }
 
diff --git a/libraries/extractor/src/main/java/androidx/media3/extractor/ts/H264Reader.java b/libraries/extractor/src/main/java/androidx/media3/extractor/ts/H264Reader.java
index a86c81f..9f5a778 100644
--- a/libraries/extractor/src/main/java/androidx/media3/extractor/ts/H264Reader.java
+++ b/libraries/extractor/src/main/java/androidx/media3/extractor/ts/H264Reader.java
@@ -25,6 +25,7 @@ import androidx.media3.common.ColorInfo;
 import androidx.media3.common.Format;
 import androidx.media3.common.MimeTypes;
 import androidx.media3.common.util.CodecSpecificDataUtil;
+import androidx.media3.common.util.Log;
 import androidx.media3.common.util.ParsableByteArray;
 import androidx.media3.common.util.UnstableApi;
 import androidx.media3.common.util.Util;
@@ -124,6 +125,8 @@ public final class H264Reader implements ElementaryStreamReader {
   public void packetStarted(long pesTimeUs, @TsPayloadReader.Flags int flags) {
     this.pesTimeUs = pesTimeUs;
     randomAccessIndicator |= (flags & FLAG_RANDOM_ACCESS_INDICATOR) != 0;
+    TsExtractor.debugH264PesStart(
+        pesTimeUs, flags, randomAccessIndicator);
   }
 
   @Override
@@ -196,6 +199,13 @@ public final class H264Reader implements ElementaryStreamReader {
 
   @RequiresNonNull("sampleReader")
   private void startNalUnit(long position, int nalUnitType, long pesTimeUs) {
+    TsExtractor.debugH264Nal(
+        nalUnitType,
+        position,
+        pesTimeUs,
+        randomAccessIndicator,
+        allowNonIdrKeyframes,
+        detectAccessUnits);
     if (!hasOutputFormat || sampleReader.needsSpsPps()) {
       sps.startNalUnit(nalUnitType);
       pps.startNalUnit(nalUnitType);
@@ -277,6 +287,7 @@ public final class H264Reader implements ElementaryStreamReader {
     }
     boolean sampleIsKeyFrame = sampleReader.endNalUnit(position, offset, hasOutputFormat);
     if (sampleIsKeyFrame) {
+      TsExtractor.debugH264Keyframe(position, pesTimeUs);
       // This is either an IDR frame or the first I-frame since the random access indicator, so mark
       // it as a keyframe. Clear the flag so that subsequent non-IDR I-frames are not marked as
       // keyframes until we see another random access indicator.
@@ -539,6 +550,7 @@ public final class H264Reader implements ElementaryStreamReader {
       @C.BufferFlags int flags = sampleIsKeyframe ? C.BUFFER_FLAG_KEY_FRAME : 0;
       int size = (int) (nalUnitStartPosition - samplePosition);
       output.sampleMetadata(sampleTimeUs, flags, size, offset, null);
+      TsExtractor.debugSampleOutput(sampleTimeUs, sampleIsKeyframe, size);
     }
 
     private static final class SliceHeaderData {
diff --git a/libraries/extractor/src/main/java/androidx/media3/extractor/ts/PesReader.java b/libraries/extractor/src/main/java/androidx/media3/extractor/ts/PesReader.java
index a13bf74..ee0dce1 100644
--- a/libraries/extractor/src/main/java/androidx/media3/extractor/ts/PesReader.java
+++ b/libraries/extractor/src/main/java/androidx/media3/extractor/ts/PesReader.java
@@ -90,6 +90,21 @@ public final class PesReader implements TsPayloadReader {
     // Asserts init has been called.
     checkNotNull(timestampAdjuster);
 
+    if (data.bytesLeft() > 0 && reader instanceof H264Reader) {
+      boolean logVideoPusi = TsExtractor.debugVideoPesFlags(flags);
+
+      if (logVideoPusi) {
+        TsExtractor.debugPes(
+            "VIDEO_PUSI"
+                + " state=" + stateName(state)
+                + " bytesRead=" + bytesRead
+                + " payloadSize=" + payloadSize
+                + " timeUs=" + timeUs
+                + " randomAccess="
+                + ((flags & FLAG_RANDOM_ACCESS_INDICATOR) != 0));
+      }
+    }
+
     if ((flags & FLAG_PAYLOAD_UNIT_START_INDICATOR) != 0) {
       switch (state) {
         case STATE_FINDING_HEADER:
@@ -187,6 +202,16 @@ public final class PesReader implements TsPayloadReader {
     bytesRead = 0;
   }
 
+  private static String stateName(int state) {
+    switch (state) {
+      case STATE_FINDING_HEADER: return "FINDING_HEADER";
+      case STATE_READING_HEADER: return "READING_HEADER";
+      case STATE_READING_HEADER_EXTENSION: return "READING_HEADER_EXTENSION";
+      case STATE_READING_BODY: return "READING_BODY";
+      default: return "UNKNOWN(" + state + ")";
+    }
+  }
+
   /**
    * Continues a read from the provided {@code source} into a given {@code target}. It's assumed
    * that the data should be written into {@code target} starting from an offset of zero.
@@ -216,6 +241,15 @@ public final class PesReader implements TsPayloadReader {
     pesScratch.setPosition(0);
     int startCodePrefix = pesScratch.readBits(24);
     if (startCodePrefix != 0x000001) {
+      TsExtractor.debugBadPesStartCode();
+      TsExtractor.debugPes(
+          "BAD_START_CODE prefix=" + startCodePrefix
+              + " hex=0x" + Integer.toHexString(startCodePrefix)
+              + " reader=" + reader.getClass().getSimpleName()
+              + " state=" + stateName(state)
+              + " bytesRead=" + bytesRead
+              + " payloadSize=" + payloadSize
+              + " timeUs=" + timeUs);
       Log.w(TAG, "Unexpected start code prefix: " + startCodePrefix);
       payloadSize = C.LENGTH_UNSET;
       return false;
diff --git a/libraries/extractor/src/main/java/androidx/media3/extractor/ts/TsExtractor.java b/libraries/extractor/src/main/java/androidx/media3/extractor/ts/TsExtractor.java
index 4f27e9b..cfa6f66 100644
--- a/libraries/extractor/src/main/java/androidx/media3/extractor/ts/TsExtractor.java
+++ b/libraries/extractor/src/main/java/androidx/media3/extractor/ts/TsExtractor.java
@@ -29,6 +29,7 @@ import androidx.annotation.Nullable;
 import androidx.media3.common.C;
 import androidx.media3.common.MimeTypes;
 import androidx.media3.common.ParserException;
+import androidx.media3.common.util.Log;
 import androidx.media3.common.util.NullableType;
 import androidx.media3.common.util.ParsableBitArray;
 import androidx.media3.common.util.ParsableByteArray;
@@ -158,6 +159,279 @@ public final class TsExtractor implements Extractor {
   private static final long HEVC_FORMAT_IDENTIFIER = 0x48455643;
 
   private static final int BUFFER_SIZE = TS_PACKET_SIZE * 50;
+
+  private static final ThreadLocal<DebugSegment> DEBUG_SEGMENT = new ThreadLocal<>();
+
+  private static final int DEBUG_INITIAL_TS_PACKETS = 10;
+  private static final int DEBUG_INITIAL_NALS = 5;
+  private static final int DEBUG_INITIAL_SAMPLES = 5;
+  private static final int DEBUG_INITIAL_VIDEO_PES = 2;
+
+  private static final class DebugSegment {
+    final long sequence;
+    final String uri;
+
+    long tsPacketsTotal;
+    int pesBadStartCodes;
+    int videoPusiCount;
+    int videoRandomAccessCount;
+    int idrCount;
+    int samplesOutput;
+    int keyframesOutput;
+
+    int nalsSeen;
+    int videoPesStartsLogged;
+    boolean loggedTsBoundaryAfterInitial;
+
+    DebugSegment(long sequence, String uri) {
+      this.sequence = sequence;
+      this.uri = uri;
+    }
+  }
+
+  public static void debugBeginHlsSegment(long sequence, String uri) {
+    DEBUG_SEGMENT.set(new DebugSegment(sequence, uri));
+    Log.d(
+        "MF_HLS_TRACE",
+        "========== HLS_SEGMENT_START seq="
+            + sequence
+            + " uri="
+            + uri
+            + " ==========");
+  }
+
+  public static void debugEndHlsSegment() {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment != null) {
+      Log.d(
+          "MF_HLS_TRACE",
+          "========== HLS_SEGMENT_END"
+              + " seq="
+              + segment.sequence
+              + " tsPacketsTotal="
+              + segment.tsPacketsTotal
+              + " pesBadStartCodes="
+              + segment.pesBadStartCodes
+              + " videoPusiCount="
+              + segment.videoPusiCount
+              + " videoRandomAccessCount="
+              + segment.videoRandomAccessCount
+              + " idrCount="
+              + segment.idrCount
+              + " samplesOutput="
+              + segment.samplesOutput
+              + " keyframesOutput="
+              + segment.keyframesOutput
+              + " ==========");
+    }
+    DEBUG_SEGMENT.remove();
+  }
+
+  static void debugTsPacketSeen() {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment != null) {
+      segment.tsPacketsTotal++;
+    }
+  }
+
+  static boolean debugVideoPesFlags(int flags) {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment == null) {
+      return false;
+    }
+
+    boolean firstVideoPusi = false;
+
+    if ((flags & FLAG_PAYLOAD_UNIT_START_INDICATOR) != 0) {
+      segment.videoPusiCount++;
+      firstVideoPusi = segment.videoPusiCount == 1;
+    }
+
+    if ((flags & TsPayloadReader.FLAG_RANDOM_ACCESS_INDICATOR) != 0) {
+      segment.videoRandomAccessCount++;
+    }
+
+    return firstVideoPusi;
+  }
+
+  static void debugBadPesStartCode() {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment != null) {
+      segment.pesBadStartCodes++;
+    }
+  }
+
+  static void debugPes(String message) {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment == null) {
+      return;
+    }
+    Log.d("MF_PES_TRACE", "seq=" + segment.sequence + " " + message);
+  }
+
+  static void debugH264PesStart(long pesTimeUs, int flags, boolean randomAccess) {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment == null) {
+      return;
+    }
+
+    boolean important = randomAccess;
+
+    if (segment.videoPesStartsLogged >= DEBUG_INITIAL_VIDEO_PES && !important) {
+      return;
+    }
+
+    segment.videoPesStartsLogged++;
+
+    Log.d(
+        "MF_H264_TRACE",
+        "seq="
+            + segment.sequence
+            + " H264_PES_START"
+            + " timeUs="
+            + pesTimeUs
+            + " flags=0x"
+            + Integer.toHexString(flags)
+            + " randomAccess="
+            + randomAccess);
+  }
+
+  static void debugH264Nal(
+      int nalType,
+      long position,
+      long pesTimeUs,
+      boolean randomAccess,
+      boolean allowNonIdr,
+      boolean detectAccessUnits) {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment == null) {
+      return;
+    }
+
+    segment.nalsSeen++;
+
+    if (nalType == 5) {
+      segment.idrCount++;
+    }
+
+    boolean importantNal =
+        nalType == 5
+            || nalType == 7
+            || nalType == 8;
+
+    if (segment.nalsSeen > DEBUG_INITIAL_NALS && !importantNal) {
+      return;
+    }
+
+    Log.d(
+        "MF_H264_TRACE",
+        "seq="
+            + segment.sequence
+            + " NAL type="
+            + nalType
+            + " position="
+            + position
+            + " pesTimeUs="
+            + pesTimeUs
+            + " randomAccess="
+            + randomAccess
+            + " allowNonIdr="
+            + allowNonIdr
+            + " detectAccessUnits="
+            + detectAccessUnits);
+  }
+
+  static void debugH264Keyframe(long position, long pesTimeUs) {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment != null) {
+      Log.d(
+          "MF_H264_TRACE",
+          "seq="
+              + segment.sequence
+              + " KEYFRAME position="
+              + position
+              + " pesTimeUs="
+              + pesTimeUs);
+    }
+  }
+
+  static void debugSampleOutput(long timeUs, boolean keyframe, int size) {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment == null) {
+      return;
+    }
+
+    segment.samplesOutput++;
+
+    if (keyframe) {
+      segment.keyframesOutput++;
+    }
+
+    if (segment.samplesOutput <= DEBUG_INITIAL_SAMPLES || keyframe) {
+      Log.d(
+          "MF_H264_TRACE",
+          "seq="
+              + segment.sequence
+              + " SAMPLE_OUTPUT number="
+              + segment.samplesOutput
+              + " timeUs="
+              + timeUs
+              + " keyframe="
+              + keyframe
+              + " size="
+              + size);
+    }
+  }
+
+  private static void debugTsPacket(
+      int pid,
+      boolean payloadUnitStart,
+      boolean adaptationFieldExists,
+      boolean payloadExists,
+      int continuityCounter,
+      boolean randomAccessIndicator) {
+    DebugSegment segment = DEBUG_SEGMENT.get();
+    if (segment == null) {
+      return;
+    }
+
+    boolean initialPacket =
+        segment.tsPacketsTotal <= DEBUG_INITIAL_TS_PACKETS;
+
+    boolean firstImportantBoundaryAfterInitial =
+        !initialPacket
+            && payloadUnitStart
+            && randomAccessIndicator
+            && !segment.loggedTsBoundaryAfterInitial;
+
+    if (!initialPacket && !firstImportantBoundaryAfterInitial) {
+      return;
+    }
+
+    if (firstImportantBoundaryAfterInitial) {
+      segment.loggedTsBoundaryAfterInitial = true;
+    }
+
+    Log.d(
+        "MF_TS_TRACE",
+        "seq="
+            + segment.sequence
+            + " packet="
+            + segment.tsPacketsTotal
+            + " pid="
+            + pid
+            + " pusi="
+            + payloadUnitStart
+            + " adaptation="
+            + adaptationFieldExists
+            + " payload="
+            + payloadExists
+            + " cc="
+            + continuityCounter
+            + " randomAccess="
+            + randomAccessIndicator);
+  }
+
   private static final int SNIFF_TS_PACKET_COUNT = 5;
 
   private final @Mode int mode;
@@ -477,6 +751,9 @@ public final class TsExtractor implements Extractor {
       tsPacketBuffer.setPosition(endOfPacket);
       return RESULT_CONTINUE;
     }
+
+    debugTsPacketSeen();
+
     packetHeaderFlags |= (tsPacketHeader & 0x400000) != 0 ? FLAG_PAYLOAD_UNIT_START_INDICATOR : 0;
     // Ignoring transport_priority (tsPacketHeader & 0x200000)
     int pid = (tsPacketHeader & 0x1FFF00) >> 8;
@@ -520,6 +797,13 @@ public final class TsExtractor implements Extractor {
     // Read the payload.
     boolean wereTracksEnded = tracksEnded;
     if (shouldConsumePacketPayload(pid)) {
+      debugTsPacket(
+          pid,
+          (packetHeaderFlags & FLAG_PAYLOAD_UNIT_START_INDICATOR) != 0,
+          adaptationFieldExists,
+          payloadExists,
+          tsPacketHeader & 0xF,
+          (packetHeaderFlags & TsPayloadReader.FLAG_RANDOM_ACCESS_INDICATOR) != 0);
       tsPacketBuffer.setLimit(endOfPacket);
       payloadReader.consume(tsPacketBuffer, packetHeaderFlags);
       tsPacketBuffer.setLimit(limit);

diff --git a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/ExoPlayerImplInternal.java b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/ExoPlayerImplInternal.java
index 88f9114..b6fb908 100644
--- a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/ExoPlayerImplInternal.java
+++ b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/ExoPlayerImplInternal.java
@@ -651,6 +651,15 @@ import java.util.Objects;
 
   @Override
   public void onContinueLoadingRequested(MediaPeriod source) {
+    Log.d(
+        "MF_EXO_CORE",
+        "CORE_CALLBACK_ENQUEUE"
+            + " sourceId="
+            + System.identityHashCode(source)
+            + " sourceClass="
+            + source.getClass().getSimpleName()
+            + " onPlaybackThread="
+            + (Looper.myLooper() == playbackLooper));
     handler.obtainMessage(MSG_SOURCE_CONTINUE_LOADING_REQUESTED, source).sendToTarget();
   }
 
@@ -3132,11 +3141,41 @@ import java.util.Objects;
   }
 
   private void handleContinueLoadingRequested(MediaPeriod mediaPeriod) {
+    Log.d(
+        "MF_EXO_CORE",
+        "CORE_CALLBACK_HANDLE"
+            + " sourceId="
+            + System.identityHashCode(mediaPeriod)
+            + " sourceClass="
+            + mediaPeriod.getClass().getSimpleName());
+
     if (queue.isLoading(mediaPeriod)) {
+      Log.d(
+          "MF_EXO_CORE",
+          "CORE_CALLBACK_MAYBE_CONTINUE"
+              + " sourceId="
+              + System.identityHashCode(mediaPeriod));
       queue.reevaluateBuffer(rendererPositionUs);
       maybeContinueLoading();
     } else if (queue.isPreloading(mediaPeriod)) {
+      Log.d(
+          "MF_EXO_CORE",
+          "CORE_CALLBACK_PRELOAD"
+              + " sourceId="
+              + System.identityHashCode(mediaPeriod));
       maybeContinuePreloading();
+    } else {
+      @Nullable MediaPeriodHolder loadingPeriodHolder = queue.getLoadingPeriod();
+      Log.d(
+          "MF_EXO_CORE",
+          "CORE_CALLBACK_IGNORED"
+              + " sourceId="
+              + System.identityHashCode(mediaPeriod)
+              + " loadingId="
+              + (loadingPeriodHolder == null
+                  ? "null"
+                  : Integer.toString(
+                      System.identityHashCode(loadingPeriodHolder.mediaPeriod))));
     }
   }
 
@@ -3170,15 +3209,31 @@ import java.util.Objects;
   }
 
   private void maybeContinueLoading() {
+    boolean previousShouldContinueLoading = shouldContinueLoading;
     shouldContinueLoading = shouldContinueLoading();
     if (shouldContinueLoading) {
       MediaPeriodHolder loadingPeriod = checkNotNull(queue.getLoadingPeriod());
-      loadingPeriod.continueLoading(
+      long playbackPositionUs = loadingPeriod.toPeriodTime(rendererPositionUs);
+      LoadingInfo loadingInfo =
           new LoadingInfo.Builder()
-              .setPlaybackPositionUs(loadingPeriod.toPeriodTime(rendererPositionUs))
+              .setPlaybackPositionUs(playbackPositionUs)
               .setPlaybackSpeed(mediaClock.getPlaybackParameters().speed)
               .setLastRebufferRealtimeMs(lastRebufferRealtimeMs)
-              .build());
+              .build();
+
+      if (playbackPositionUs >= 0 && playbackPositionUs < 10_000_000L) {
+        Log.d(
+            "MF_EXO_CORE",
+            "CORE_CONTINUE_CALL"
+                + " periodId="
+                + System.identityHashCode(loadingPeriod.mediaPeriod)
+                + " previousLatch="
+                + previousShouldContinueLoading
+                + " playbackUs="
+                + playbackPositionUs);
+      }
+
+      loadingPeriod.continueLoading(loadingInfo);
     }
     updateIsLoading();
   }
@@ -3212,6 +3267,7 @@ import java.util.Objects;
             targetLiveOffsetUs,
             lastRebufferRealtimeMs);
     boolean shouldContinueLoading = loadControl.shouldContinueLoading(loadParameters);
+    boolean initialShouldContinueLoading = shouldContinueLoading;
     MediaPeriodHolder playingPeriodHolder = queue.getPlayingPeriod();
     if (!shouldContinueLoading
         && playingPeriodHolder.prepared
@@ -3223,13 +3279,53 @@ import java.util.Objects;
           playbackInfo.positionUs, /* toKeyframe= */ false);
       shouldContinueLoading = loadControl.shouldContinueLoading(loadParameters);
     }
+
+    if (playbackPositionUs >= 0 && playbackPositionUs < 10_000_000L) {
+      Log.d(
+          "MF_EXO_CORE",
+          "LOAD_CONTROL_DECISION"
+              + " playbackUs="
+              + playbackPositionUs
+              + " bufferedUs="
+              + bufferedDurationUs
+              + " initial="
+              + initialShouldContinueLoading
+              + " result="
+              + shouldContinueLoading);
+    }
+
     return shouldContinueLoading;
   }
 
   private boolean isLoadingPossible(@Nullable MediaPeriodHolder mediaPeriodHolder) {
-    return mediaPeriodHolder != null
-        && !mediaPeriodHolder.hasLoadingError()
-        && mediaPeriodHolder.getNextLoadPositionUs() != C.TIME_END_OF_SOURCE;
+    if (mediaPeriodHolder == null) {
+      Log.d(
+          "MF_EXO_CORE",
+          "LOADING_POSSIBLE result=false reason=noLoadingPeriod");
+      return false;
+    }
+
+    if (mediaPeriodHolder.hasLoadingError()) {
+      Log.d(
+          "MF_EXO_CORE",
+          "LOADING_POSSIBLE"
+              + " result=false reason=loadingError"
+              + " periodId="
+              + System.identityHashCode(mediaPeriodHolder.mediaPeriod));
+      return false;
+    }
+
+    if (mediaPeriodHolder.getNextLoadPositionUs() == C.TIME_END_OF_SOURCE) {
+      Log.d(
+          "MF_EXO_CORE",
+          "LOADING_POSSIBLE"
+              + " result=false reason=endOfSource"
+              + " periodId="
+              + System.identityHashCode(mediaPeriodHolder.mediaPeriod));
+      return false;
+    }
+
+    return true;
   }
 
   private void updateIsLoading() {
diff --git a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/ClippingMediaPeriod.java b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/ClippingMediaPeriod.java
index 014ad17..f726cf2 100644
--- a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/ClippingMediaPeriod.java
+++ b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/ClippingMediaPeriod.java
@@ -26,6 +26,7 @@ import androidx.media3.common.Format;
 import androidx.media3.common.MimeTypes;
 import androidx.media3.common.StreamKey;
 import androidx.media3.common.util.ExperimentalApi;
+import androidx.media3.common.util.Log;
 import androidx.media3.common.util.NullableType;
 import androidx.media3.common.util.UnstableApi;
 import androidx.media3.common.util.Util;
@@ -289,6 +290,13 @@ public final class ClippingMediaPeriod implements MediaPeriod, MediaPeriod.Callb
 
   @Override
   public boolean continueLoading(LoadingInfo loadingInfo) {
+    Log.d(
+        "MF_EXO_CORE",
+        "CLIPPING_CONTINUE_ENTER"
+            + " thisId="
+            + System.identityHashCode(this)
+            + " childId="
+            + System.identityHashCode(mediaPeriod));
     return mediaPeriod.continueLoading(loadingInfo);
   }
 
@@ -309,6 +317,17 @@ public final class ClippingMediaPeriod implements MediaPeriod, MediaPeriod.Callb
 
   @Override
   public void onContinueLoadingRequested(MediaPeriod source) {
+    Log.d(
+        "MF_EXO_CORE",
+        "CLIPPING_CALLBACK_ENTER"
+            + " thisId="
+            + System.identityHashCode(this)
+            + " sourceId="
+            + System.identityHashCode(source)
+            + " childId="
+            + System.identityHashCode(mediaPeriod)
+            + " sourceMatchesChild="
+            + (source == mediaPeriod));
     checkNotNull(callback).onContinueLoadingRequested(this);
   }
 
diff --git a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/MaskingMediaPeriod.java b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/MaskingMediaPeriod.java
index 8f34681..88f5a97 100644
--- a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/MaskingMediaPeriod.java
+++ b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/MaskingMediaPeriod.java
@@ -22,6 +22,7 @@ import static com.google.common.base.Preconditions.checkState;
 import androidx.annotation.Nullable;
 import androidx.media3.common.C;
 import androidx.media3.common.StreamKey;
+import androidx.media3.common.util.Log;
 import androidx.media3.common.util.NullableType;
 import androidx.media3.common.util.UnstableApi;
 import androidx.media3.exoplayer.LoadingInfo;
@@ -233,6 +234,15 @@ public final class MaskingMediaPeriod implements MediaPeriod, MediaPeriod.Callba
 
   @Override
   public boolean continueLoading(LoadingInfo loadingInfo) {
+    Log.d(
+        "MF_EXO_CORE",
+        "MASKING_CONTINUE_ENTER"
+            + " thisId="
+            + System.identityHashCode(this)
+            + " childId="
+            + (mediaPeriod == null
+                ? "null"
+                : Integer.toString(System.identityHashCode(mediaPeriod))));
     return mediaPeriod != null && mediaPeriod.continueLoading(loadingInfo);
   }
 
@@ -243,6 +253,19 @@ public final class MaskingMediaPeriod implements MediaPeriod, MediaPeriod.Callba
 
   @Override
   public void onContinueLoadingRequested(MediaPeriod source) {
+    Log.d(
+        "MF_EXO_CORE",
+        "MASKING_CALLBACK_ENTER"
+            + " thisId="
+            + System.identityHashCode(this)
+            + " sourceId="
+            + System.identityHashCode(source)
+            + " childId="
+            + (mediaPeriod == null
+                ? "null"
+                : Integer.toString(System.identityHashCode(mediaPeriod)))
+            + " sourceMatchesChild="
+            + (source == mediaPeriod));
     castNonNull(callback).onContinueLoadingRequested(this);
   }
 
diff --git a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/CompositeSequenceableLoader.java b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/CompositeSequenceableLoader.java
index 3def7e4..2c25ace 100644
--- a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/CompositeSequenceableLoader.java
+++ b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/CompositeSequenceableLoader.java
@@ -19,6 +19,7 @@ import static com.google.common.base.Preconditions.checkArgument;
 import static java.lang.Math.min;
 
 import androidx.media3.common.C;
+import androidx.media3.common.util.Log;
 import androidx.media3.common.util.UnstableApi;
 import androidx.media3.exoplayer.LoadingInfo;
 import com.google.common.collect.ImmutableList;
@@ -114,25 +115,127 @@ public final class CompositeSequenceableLoader implements SequenceableLoader {
 
   @Override
   public boolean continueLoading(LoadingInfo loadingInfo) {
+    boolean traceStartup =
+        loadingInfo.playbackPositionUs >= 0
+            && loadingInfo.playbackPositionUs < 10_000_000L;
+
+    if (traceStartup) {
+      Log.d(
+          "MF_EXO_MERGE",
+          "COMPOSITE_ENTER"
+              + " playbackUs="
+              + loadingInfo.playbackPositionUs
+              + " loaders="
+              + loadersWithTrackTypes.size());
+    }
+
     boolean madeProgress = false;
     boolean madeProgressThisIteration;
+    int iteration = 0;
+
     do {
       madeProgressThisIteration = false;
+
+      // This is the same single global next-position lookup used upstream.
       long nextLoadPositionUs = getNextLoadPositionUs();
+
+      if (traceStartup) {
+        Log.d(
+            "MF_EXO_MERGE",
+            "COMPOSITE_ITERATION"
+                + " iteration="
+                + iteration
+                + " minNextLoadUs="
+                + nextLoadPositionUs);
+      }
+
       if (nextLoadPositionUs == C.TIME_END_OF_SOURCE) {
         break;
       }
+
       for (int i = 0; i < loadersWithTrackTypes.size(); i++) {
-        long loaderNextLoadPositionUs = loadersWithTrackTypes.get(i).getNextLoadPositionUs();
+        SequenceableLoaderWithTrackTypes loaderWithTrackTypes =
+            loadersWithTrackTypes.get(i);
+
+        // Same one-per-child next-position lookup used upstream.
+        long loaderNextLoadPositionUs =
+            loaderWithTrackTypes.getNextLoadPositionUs();
+
         boolean isLoaderBehind =
             loaderNextLoadPositionUs != C.TIME_END_OF_SOURCE
                 && loaderNextLoadPositionUs <= loadingInfo.playbackPositionUs;
-        if (loaderNextLoadPositionUs == nextLoadPositionUs || isLoaderBehind) {
-          madeProgressThisIteration |= loadersWithTrackTypes.get(i).continueLoading(loadingInfo);
+
+        boolean selected =
+            loaderNextLoadPositionUs == nextLoadPositionUs || isLoaderBehind;
+
+        // Requested diagnostic state query. SequenceableLoaderWithTrackTypes
+        // simply delegates this to the underlying child loader.
+        boolean childIsLoading =
+            traceStartup && loaderWithTrackTypes.isLoading();
+
+        if (traceStartup) {
+          Log.d(
+              "MF_EXO_MERGE",
+              "COMPOSITE_CHILD"
+                  + " iteration="
+                  + iteration
+                  + " index="
+                  + i
+                  + " loaderId="
+                  + System.identityHashCode(loaderWithTrackTypes.loader)
+                  + " loaderClass="
+                  + loaderWithTrackTypes.loader.getClass().getSimpleName()
+                  + " trackTypes="
+                  + loaderWithTrackTypes.getTrackTypes()
+                  + " nextLoadUs="
+                  + loaderNextLoadPositionUs
+                  + " minNextLoadUs="
+                  + nextLoadPositionUs
+                  + " behind="
+                  + isLoaderBehind
+                  + " selected="
+                  + selected
+                  + " isLoading="
+                  + childIsLoading);
+        }
+
+        if (selected) {
+          // Exactly one continueLoading() call, just like upstream.
+          boolean childMadeProgress =
+              loaderWithTrackTypes.continueLoading(loadingInfo);
+
+          if (traceStartup) {
+            Log.d(
+                "MF_EXO_MERGE",
+                "COMPOSITE_CHILD_CONTINUE"
+                    + " iteration="
+                    + iteration
+                    + " index="
+                    + i
+                    + " loaderId="
+                    + System.identityHashCode(loaderWithTrackTypes.loader)
+                    + " result="
+                    + childMadeProgress);
+          }
+
+          madeProgressThisIteration |= childMadeProgress;
         }
       }
+
       madeProgress |= madeProgressThisIteration;
+      iteration++;
     } while (madeProgressThisIteration);
+
+    if (traceStartup) {
+      Log.d(
+          "MF_EXO_MERGE",
+          "COMPOSITE_RETURN"
+              + " iterations="
+              + iteration
+              + " madeProgress="
+              + madeProgress);
+    }
+
     return madeProgress;
   }
 
diff --git a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/MergingMediaPeriod.java b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/MergingMediaPeriod.java
index b9bff4b..5ea2997 100644
--- a/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/MergingMediaPeriod.java
+++ b/libraries/exoplayer/src/main/java/androidx/media3/exoplayer/source/MergingMediaPeriod.java
@@ -23,6 +23,7 @@ import androidx.media3.common.C;
 import androidx.media3.common.Format;
 import androidx.media3.common.StreamKey;
 import androidx.media3.common.TrackGroup;
+import androidx.media3.common.util.Log;
 import androidx.media3.common.util.NullableType;
 import androidx.media3.exoplayer.LoadingInfo;
 import androidx.media3.exoplayer.SeekParameters;
@@ -202,15 +203,58 @@ import java.util.List;
 
   @Override
   public boolean continueLoading(LoadingInfo loadingInfo) {
+    boolean traceStartup =
+        loadingInfo.playbackPositionUs >= 0
+            && loadingInfo.playbackPositionUs < 10_000_000L;
+
+    if (traceStartup) {
+      Log.d(
+          "MF_EXO_MERGE",
+          "MERGE_CONTINUE_ENTER"
+              + " thisId="
+              + System.identityHashCode(this)
+              + " playbackUs="
+              + loadingInfo.playbackPositionUs
+              + " pendingPreparation="
+              + childrenPendingPreparation.size()
+              + " enabledPeriods="
+              + enabledPeriods.length);
+    }
+
     if (!childrenPendingPreparation.isEmpty()) {
       // Preparation is still going on.
       int childrenPendingPreparationSize = childrenPendingPreparation.size();
+
       for (int i = 0; i < childrenPendingPreparationSize; i++) {
         childrenPendingPreparation.get(i).continueLoading(loadingInfo);
       }
+
+      if (traceStartup) {
+        Log.d(
+            "MF_EXO_MERGE",
+            "MERGE_CONTINUE_RETURN"
+                + " mode=preparing"
+                + " result=false"
+                + " pendingPreparation="
+                + childrenPendingPreparationSize);
+      }
+
       return false;
     } else {
-      return compositeSequenceableLoader.continueLoading(loadingInfo);
+      boolean result = compositeSequenceableLoader.continueLoading(loadingInfo);
+
+      if (traceStartup) {
+        Log.d(
+            "MF_EXO_MERGE",
+            "MERGE_CONTINUE_RETURN"
+                + " mode=composite"
+                + " result="
+                + result
+                + " enabledPeriods="
+                + enabledPeriods.length);
+      }
+
+      return result;
     }
   }
 
@@ -328,7 +372,41 @@ import java.util.List;
   }
 
   @Override
-  public void onContinueLoadingRequested(MediaPeriod ignored) {
+  public void onContinueLoadingRequested(MediaPeriod source) {
+    int sourceIndex = C.INDEX_UNSET;
+    for (int i = 0; i < periods.length; i++) {
+      if (periods[i] == source) {
+        sourceIndex = i;
+        break;
+      }
+    }
+
+    int enabledIndex = C.INDEX_UNSET;
+    for (int i = 0; i < enabledPeriods.length; i++) {
+      if (enabledPeriods[i] == source) {
+        enabledIndex = i;
+        break;
+      }
+    }
+
+    Log.d(
+        "MF_EXO_MERGE",
+        "MERGE_CALLBACK_ENTER"
+            + " thisId="
+            + System.identityHashCode(this)
+            + " sourceId="
+            + System.identityHashCode(source)
+            + " sourceClass="
+            + source.getClass().getSimpleName()
+            + " sourceIndex="
+            + sourceIndex
+            + " enabledIndex="
+            + enabledIndex
+            + " pendingPreparation="
+            + childrenPendingPreparation.size()
+            + " enabledPeriods="
+            + enabledPeriods.length);
+
     checkNotNull(callback).onContinueLoadingRequested(this);
   }
 
